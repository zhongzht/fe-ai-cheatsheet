# 迁移批次对照表（Vue2 → Vue3）

> 按依赖倒序分批迁移 · 每批 AI 指令模板 + 人工确认清单  
> 作者：柯里 · 公众号「前端AI手记」

## 分批原则

从叶子到根倒序：**纯展示叶子组件 → 业务组件 → 页面 → 路由 / store / 全局插件**。底层先稳定，上层才敢改。如果先把页面改了，引用的底层还是 Vue2 写法，编译直接红一片，你分不清新错旧错。

## 批次对照

| 批次 | 范围 | 检查点 | AI 指令要点 |
| --- | --- | --- | --- |
| 1 叶子组件 | 纯展示 UI | props / 事件是否保留 | 只改语法，不动结构 |
| 2 业务组件 | 含逻辑 | mixin 是否拆解干净 | 禁止顺手重构 |
| 3 页面 | 路由挂载 | 生命周期映射 | 输出 diff |
| 4 全局 | store / 插件 / 指令 | 注册方式 | 人工把关 |

## 每批 AI 指令模板

```
只改当前批次的以下文件：[路径列表]
不要碰其它文件。
把 Vue2 写法迁移到 Vue3：
- this.xxx → ref / 响应式
- 生命周期 beforeDestroy → onUnmounted 等
- filter → computed / method
- mixin → composable 或就地展开
输出：1) 每个文件的 diff 2) 每处改动说明 3) 拿不准的边界标 TODO
```

## 人工确认清单（每批必过）

- [ ] diff 是否只动了当前批次文件
- [ ] 是否顺手「重构」了不该动的代码
- [ ] mixin / this.$refs / mapState 是否处理干净
- [ ] 边界与异常分支有没有漏改
- [ ] 跑一遍：构建通过 + 自测关键路径

---

资料链接：https://github.com/zhongzht/fe-ai-cheatsheet
