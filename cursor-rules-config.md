# 前端 .cursorrules 完整配置

> 作者：柯里 · 公众号「前端 × AI 实战」
> 适用：Vue 3 + TypeScript + Vite 中后台项目，其他技术栈可照结构改

## 怎么用

1. 把 `.cursorrules` 文件放进项目根目录（和 package.json 同级），Cursor 会自动读取。
2. 打开一个新对话，AI 会先读这份规则再回答。
3. 用一阵子后，把 review 中反复出现的问题追加进对应区块——它是活的。

## 完整配置（40 行）

```text
# ============================================
# 项目画像 —— 告诉 AI 它活在哪个项目里
# ============================================

Vue 3.4 + TypeScript + Vite + Pinia 的中后台管理系统
构建与检查命令：pnpm dev / pnpm typecheck / pnpm lint
UI：优先使用 src/components/ui 内部组件库，禁止引入 Element Plus 等第三方 UI 库
接口请求：一律通过 src/api 下按模块封装好的函数，页面与组件禁止直接写 axios/fetch
路由页面放 src/pages，可复用组件放 src/components
状态：跨页共享状态进 Pinia store，组件内局部状态用 ref 即可

# ============================================
# 代码风格 —— 与团队规范保持一致
# ============================================

所有 Vue 组件使用 <script setup lang="ts"> 写法
props 一律用 defineProps 的纯类型声明，不要写运行时校验
emit 事件用 defineEmits 类型声明，禁止用 any 定义载荷
布尔变量用 is/has/can 开头，事件处理函数用 handle/on 开头
颜色、间距、圆角优先使用主题 CSS 变量，变量没有的才写具体值
样式用 scoped CSS；避免深选择器 :deep()，优先通过 props 提供样式入口
不要在组件内写与业务无关的 console.log，需要保留的调试代码要注释说明

# ============================================
# 组件约定 —— 不要重复造轮子
# ============================================

写任何组件前，先确认 components/ui 里没有现成可用的
表格列、表单项、下拉选项这类配置型数据，收敛成配置对象，不要在模板里堆大段结构
带业务含义的枚举、状态码、常量，放 src/constants 统一管理，禁止散落在组件内
通用请求逻辑、权限判断，优先抽取为 composables（src/composables）

# ============================================
# 禁止事项 —— 雷区，碰之前先问
# ============================================

禁止修改 src/api 与 src/types 下已定义的接口契约与类型，确有需要先向用户确认
禁止用 any 绕过类型错误；优先用类型收窄（联合类型、in 判断、类型守卫）
禁止新增第三方依赖；确有必要时说明替代方案与体积成本，等确认后再引
禁止重排/重写大段既有可运行代码，只做增量修改
禁止把非通用逻辑硬编码进 ui 组件库组件

# ============================================
# 工作方式 —— 像同事一样交付
# ============================================

每次改动完成后，用 3 行以内说明你改了什么、为什么改
涉及接口返回或数据结构时，先写出你假设的字段结构，再开始写代码
写完自行运行类型检查，确认无新增报错再交付
涉及权限、分页、空态、loading 等场景时，主动补齐这些状态的 UI 处理，不要只写主流程
```

## 五个区块一句话记忆

| 区块 | 回答的问题 |
| --- | --- |
| 项目画像 | 项目用什么技术栈？目录和命令约定是什么？ |
| 代码风格 | 组件怎么写、变量怎么命名、样式怎么组织？ |
| 组件约定 | 什么情况算"重复造轮子"？ |
| 禁止事项 | 哪些文件/写法不能碰？碰了要先问谁？ |
| 工作方式 | 交付前要自查什么、怎么交代改动？ |

---

资料链接：https://github.com/zhongzht/fe-ai-cheatsheet
